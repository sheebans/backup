import { moduleFor, test } from 'ember-qunit';

moduleFor(
  'model:editor/assessment/category',
  'Unit | Model | editor/assessment/category',
  {
    unit: true
  }
);

test('it exists', function(assert) {
  const model = this.subject();
  // let store = this.store();
  assert.ok(!!model);
});
